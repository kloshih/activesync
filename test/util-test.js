
const { expect } = require('chai');

const {
  isSubclass,
  superclass,
  typeOf,
  merge,
  clone,
  perform,
  wait,
} = require('../lib/util')

describe("util", () => {

  describe("Basic functionality", () => {

    it("can do something", async () => {
    })

  })

})